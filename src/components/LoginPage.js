import React from 'react';
import LoginForm from '../components/LoginForm';

const LoginPage = () => {
    return (
        <div>
            <h1>登录</h1>
            <LoginForm />
        </div>
    );
};

export default LoginPage;